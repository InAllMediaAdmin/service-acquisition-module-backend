spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:service-acquisition;DB_CLOSE_ON_EXIT=FALSE;LOCK_TIMEOUT=10000;MODE=MySQL
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: false
    baseline-on-migrate: false
    url: jdbc:h2:mem
    user: sa
    password:
    schemas: service-acquisition

